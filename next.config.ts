import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Sass/SCSS is supported out of the box (Turbopack configures sass-loader
  // automatically), so no custom loader rules are needed.
};

export default nextConfig;
